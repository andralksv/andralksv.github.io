# Mixpanel project token.
#
# This is a PUBLIC, client-side identifier — it ships in the browser bundle by
# design, so it is safe to commit and expose. It is NOT a secret.
#
# Committed (not in .gitignore) so the GitHub Actions build picks it up with zero
# extra config. To split dev/prod later, create separate Mixpanel projects and
# swap this value per environment (e.g. via .env.production / CI variables).
VITE_MIXPANEL_TOKEN=518da530fdebceb32e3a71e5a2c8993a
